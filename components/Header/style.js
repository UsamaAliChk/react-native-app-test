import {StyleSheet} from 'react-native';

const Header = StyleSheet.create({
  background: {
    display: 'flex',
    flexDirection: 'row',

    alignItems: 'center',
    height: 50,
    width: '100%',
  },
  arrow: {
    marginLeft: 15,
  },
});

export default Header;
