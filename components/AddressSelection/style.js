import {StyleSheet} from 'react-native';

const addressSelectionStyle = StyleSheet.create({
  back: {
    display: 'flex',
    flexDirection: 'row',
    marginBottom: 15,
    marginTop: 15,
  },
});

export default addressSelectionStyle;
